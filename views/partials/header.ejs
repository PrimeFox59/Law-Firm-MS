<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#4f46e5">
  <title><%= title %> - Law Firm Management</title>

  <link rel="manifest" href="/manifest.json">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

  <style>
    :root {
      --primary: #1b2433;
      --primary-accent: #4f46e5;
      --primary-soft: #eef2ff;
      --success: #16a34a;
      --warning: #f59e0b;
      --danger: #dc2626;
      --muted: #6b7280;
    }

    body {
      background: linear-gradient(135deg, #f5f7fb 0%, #eef2f7 100%);
      font-family: 'Inter', 'Segoe UI', system-ui, -apple-system, sans-serif;
      color: #0f172a;
      overflow-x: hidden;
    }

    .layout-shell { min-height: 100vh; }

    .sidebar {
      width: 88px;
      background: radial-gradient(circle at 20% 20%, rgba(79,70,229,0.12), transparent 25%),
                  linear-gradient(180deg, #111827 0%, #0b1220 100%);
      color: #e5e7eb;
      box-shadow: 4px 0 24px rgba(17,24,39,0.35);
      position: fixed;
      left: 0;
      top: 0;
      bottom: 0;
      height: 100dvh;
      padding-bottom: 10px;
      overflow-y: auto;
      z-index: 9000;
    }

    .brand-block {
      padding: 12px 0 12px;
      border-bottom: 1px solid rgba(255,255,255,0.08);
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 4px;
    }

    .brand-logo {
      width: 42px;
      height: 42px;
      border-radius: 12px;
      background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: 700;
      box-shadow: 0 8px 20px rgba(79,70,229,0.35);
    }

    .sidebar nav { display: flex; flex-direction: column; gap: 2px; overflow: visible; }
    /* Keep tooltips visible above sidebar */
    .sidebar [data-bs-toggle="tooltip"] { position: relative; z-index: 1; }
    .tooltip { z-index: 10000; }

    .sidebar .nav-link {
      color: #e5e7eb;
      border-radius: 12px;
      padding: 10px 0;
      margin: 2px 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      transition: all 0.2s ease;
      min-height: 40px;
    }

    .sidebar .nav-link i { width: 16px; font-size: 14px; }
    .sidebar .nav-text { display: none; }

    .sidebar .nav-link:hover {
      background: rgba(255,255,255,0.06);
      color: #fff;
      transform: translateX(4px);
    }

    .sidebar .nav-link.active {
      background: rgba(79,70,229,0.25);
      color: #fff;
      border: 1px solid rgba(79,70,229,0.4);
      box-shadow: 0 10px 30px rgba(79,70,229,0.25);
    }

    .content-shell {
      background: transparent;
      margin-left: 88px;
      width: calc(100% - 88px);
    }

    .topbar {
      position: sticky;
      top: 0;
      z-index: 8000;
      background: rgba(255,255,255,0.9);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid #e5e7eb;
    }

    .page-content {
      background: transparent;
    }

    .glass-card {
      background: rgba(255,255,255,0.92);
      border-radius: 16px;
      border: 1px solid rgba(226,232,240,0.7);
      box-shadow: 0 10px 40px rgba(15,23,42,0.08);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .glass-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 16px 48px rgba(15,23,42,0.12);
    }

    .card {
      border: 1px solid rgba(226,232,240,0.8);
      border-radius: 14px;
      box-shadow: 0 10px 30px rgba(15,23,42,0.06);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .card:hover { transform: translateY(-2px); box-shadow: 0 14px 36px rgba(15,23,42,0.1); }

    .btn-primary, .btn-gradient {
      background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
      border: none;
      color: #fff;
      box-shadow: 0 10px 30px rgba(79,70,229,0.35);
    }

    .btn-primary:hover, .btn-gradient:hover {
      background: linear-gradient(135deg, #4338ca 0%, #6d28d9 100%);
      color: #fff;
    }

    .table-modern thead {
      background: #f8fafc;
      border-bottom: 1px solid #e5e7eb;
    }

    .table-modern tbody tr:hover {
      background: #f9fafb;
    }

    .badge-soft {
      padding: 6px 10px;
      border-radius: 999px;
      font-weight: 600;
      border: 1px solid rgba(0,0,0,0.04);
    }

    .badge-soft-success { color: #166534; background: #ecfdf3; }
    .badge-soft-warning { color: #92400e; background: #fffbeb; }
    .badge-soft-danger { color: #991b1b; background: #fef2f2; }
    .badge-soft-info { color: #0f172a; background: #eef2ff; }

    .shadow-xs { box-shadow: 0 4px 14px rgba(15,23,42,0.06); }

    .notif-toggle {
      width: 38px;
      height: 38px;
      border-radius: 12px;
      background: rgba(255,255,255,0.8);
      border: 1px solid rgba(226,232,240,0.8);
      box-shadow: 0 6px 18px rgba(15,23,42,0.12);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      position: relative;
      transition: transform 0.15s ease, box-shadow 0.15s ease;
    }

    .notif-toggle:hover { transform: translateY(-1px); box-shadow: 0 10px 24px rgba(15,23,42,0.14); }

    .notif-panel {
      position: fixed;
      top: 72px;
      right: 32px;
      width: min(340px, calc(100vw - 32px));
      background: rgba(255,255,255,0.9);
      border: 1px solid rgba(226,232,240,0.7);
      border-radius: 16px;
      box-shadow: 0 18px 60px rgba(15,23,42,0.2);
      backdrop-filter: blur(14px);
      -webkit-backdrop-filter: blur(14px);
      z-index: 12000;
      overflow: hidden;
      pointer-events: auto;
    }

    .notif-panel-header {
      padding: 12px 16px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: rgba(79,70,229,0.06);
      border-bottom: 1px solid rgba(226,232,240,0.6);
    }

    .notif-list { max-height: 320px; overflow-y: auto; }

    .notif-item {
      padding: 12px 16px;
      display: flex;
      align-items: flex-start;
      gap: 10px;
      border-bottom: 1px solid rgba(226,232,240,0.6);
    }

    .notif-dot { width: 10px; height: 10px; border-radius: 50%; margin-top: 6px; }

    .global-search { min-width: 420px; }
    .global-search .dropdown-toggle { min-width: 120px; }
    .global-search input { min-width: 220px; }

    /* Mobile adjustments */
    @media (max-width: 991.98px) {
      .layout-shell { min-height: 100dvh; }
      .sidebar {
        width: 68px;
        height: 100dvh;
      }
      .content-shell { margin-left: 68px; width: calc(100% - 68px); }
      .topbar .container-fluid { flex-wrap: wrap; gap: 8px; }
      .global-search { min-width: unset; width: 100%; }
      .global-search .dropdown-toggle { min-width: 100px; }
      .global-search input { min-width: 0; width: 100%; }
      .topbar .d-flex.align-items-center { flex-wrap: wrap; gap: 8px; }
      .topbar .d-flex.align-items-center.gap-3 { width: 100%; justify-content: space-between; }
      .topbar .d-flex.align-items-center.position-relative { width: 100%; justify-content: flex-start; flex-wrap: wrap; gap: 8px; }
      .card { box-shadow: 0 6px 18px rgba(15,23,42,0.08); }
    }

    /* Dashboard refinements */
    .metric-card {
      border: 1px solid rgba(226,232,240,0.7);
      border-radius: 14px;
      padding: 14px 16px;
      background: linear-gradient(180deg, rgba(255,255,255,0.95) 0%, rgba(247,249,252,0.95) 100%);
      box-shadow: 0 10px 26px rgba(15,23,42,0.08);
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .metric-icon {
      width: 40px;
      height: 40px;
      border-radius: 12px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: rgba(79,70,229,0.08);
      color: #4f46e5;
      font-size: 16px;
    }

    .list-clean { margin: 0; padding: 0; list-style: none; }
    .list-clean .item {
      padding: 10px 0;
      border-bottom: 1px solid rgba(226,232,240,0.7);
      display: flex;
      justify-content: space-between;
      gap: 12px;
      align-items: center;
    }
    .list-clean .item:last-child { border-bottom: none; }

    .pill {
      padding: 6px 10px;
      border-radius: 999px;
      font-weight: 600;
      font-size: 12px;
      background: #eef2ff;
      color: #312e81;
      border: 1px solid rgba(79,70,229,0.12);
    }

    .mini-invoice { background: #fff; border: 1px solid rgba(226,232,240,0.9); border-radius: 12px; padding: 12px; }
    .mini-invoice .label { font-size: 12px; color: #6b7280; }
    .mini-invoice .value { font-weight: 700; font-size: 20px; }
    .mini-invoice .meta { font-size: 12px; }
  </style>
</head>
<body>
  <div class="layout-shell d-flex">
    <!-- Sidebar -->
    <aside class="sidebar d-flex flex-column p-3">
      <div class="brand-block d-flex align-items-center gap-3 mb-3">
        <div class="brand-logo"><i class="fas fa-balance-scale"></i></div>
      </div>

      <nav class="flex-grow-1">
        <a class="nav-link <%= title === 'Dashboard' ? 'active' : '' %>" href="/dashboard" aria-label="Dashboard" data-bs-toggle="tooltip" data-bs-placement="right" title="Dashboard"><i class="fas fa-home"></i><span class="nav-text">Dashboard</span></a>
        <a class="nav-link <%= title === 'Contacts' ? 'active' : '' %>" href="/contacts" aria-label="Contacts" data-bs-toggle="tooltip" data-bs-placement="right" title="Contacts"><i class="fas fa-address-book"></i><span class="nav-text">Contacts</span></a>
        <a class="nav-link <%= title === 'Calendar' ? 'active' : '' %>" href="/calendar" aria-label="Calendar" data-bs-toggle="tooltip" data-bs-placement="right" title="Calendar"><i class="fas fa-calendar"></i><span class="nav-text">Calendar</span></a>
        <a class="nav-link <%= title === 'Matters' || title === 'Matter Detail' ? 'active' : '' %>" href="/matters" aria-label="Matters" data-bs-toggle="tooltip" data-bs-placement="right" title="Matters"><i class="fas fa-briefcase"></i><span class="nav-text">Matters</span></a>
        <a class="nav-link <%= title === 'Tasks' ? 'active' : '' %>" href="/tasks" aria-label="Tasks" data-bs-toggle="tooltip" data-bs-placement="right" title="Tasks"><i class="fas fa-tasks"></i><span class="nav-text">Tasks</span></a>
        <a class="nav-link <%= title === 'Chat' ? 'active' : '' %>" href="/chat" aria-label="Chat" data-bs-toggle="tooltip" data-bs-placement="right" title="Chat"><i class="fas fa-comments"></i><span class="nav-text">Chat</span></a>
        <a class="nav-link <%= title === 'Documents' ? 'active' : '' %>" href="/documents" aria-label="Documents" data-bs-toggle="tooltip" data-bs-placement="right" title="Documents"><i class="fas fa-folder"></i><span class="nav-text">Documents</span></a>
        <a class="nav-link <%= title === 'Cost Journal' ? 'active' : '' %>" href="/cost-journal" aria-label="Cost Journal" data-bs-toggle="tooltip" data-bs-placement="right" title="Cost Journal"><i class="fas fa-book"></i><span class="nav-text">Cost Journal</span></a>
        <a class="nav-link <%= title.includes('Invoice') ? 'active' : '' %>" href="/invoices" aria-label="Invoices" data-bs-toggle="tooltip" data-bs-placement="right" title="Invoices"><i class="fas fa-file-invoice-dollar"></i><span class="nav-text">Invoices</span></a>
        <a class="nav-link <%= title === 'Transactions' ? 'active' : '' %>" href="/transactions" aria-label="Transactions" data-bs-toggle="tooltip" data-bs-placement="right" title="Transactions"><i class="fas fa-exchange-alt"></i><span class="nav-text">Transactions</span></a>
        <% if (user && user.account_type === 'admin') { %>
          <a class="nav-link <%= title === 'Users' ? 'active' : '' %>" href="/users" aria-label="Users" data-bs-toggle="tooltip" data-bs-placement="right" title="Users"><i class="fas fa-users"></i><span class="nav-text">Users</span></a>
        <% } %>
        <a class="nav-link <%= title.includes('Settings') ? 'active' : '' %>" href="/settings" aria-label="Settings" data-bs-toggle="tooltip" data-bs-placement="right" title="Settings"><i class="fas fa-cog"></i><span class="nav-text">Settings</span></a>
      </nav>

      <div class="mt-auto pt-2 border-top border-secondary border-opacity-25">
        <a class="nav-link" href="/auth/logout" aria-label="Logout" data-bs-toggle="tooltip" data-bs-placement="right" title="Logout"><i class="fas fa-sign-out-alt"></i></a>
      </div>
    </aside>

    <!-- Main Content -->
    <div class="flex-grow-1 d-flex flex-column content-shell">
      <!-- Top Navbar -->
      <nav class="topbar d-flex align-items-center justify-content-between px-4 py-3 shadow-xs">
        <div class="d-flex align-items-center gap-3">
          <h4 class="mb-0 fw-semibold text-dark"><%= title %></h4>
          <div class="global-search d-flex align-items-center gap-2">
            <div class="dropdown">
              <button class="btn btn-light btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false" id="globalSearchCategoryBtn" data-value="matter">
                Matter
              </button>
              <ul class="dropdown-menu">
                <li><a class="dropdown-item active" href="#" data-value="matter">Matter</a></li>
                <li><a class="dropdown-item" href="#" data-value="task">Task</a></li>
                <li><a class="dropdown-item" href="#" data-value="contact">Contact</a></li>
                <li><a class="dropdown-item" href="#" data-value="document">Document</a></li>
              </ul>
            </div>
            <input type="text" class="form-control form-control-sm" id="globalSearchInput" placeholder="Search..." aria-label="Global search">
            <button class="btn btn-primary btn-sm" id="globalSearchBtn" type="button"><i class="fas fa-search"></i></button>
          </div>
        </div>
        <div class="d-flex align-items-center gap-3 position-relative">
          <div class="d-flex align-items-center gap-2">
            <button class="btn btn-sm btn-outline-secondary" id="timerStartBtn" type="button" title="Start timer">
              <i class="fas fa-play"></i>
            </button>
            <button class="btn btn-sm btn-outline-danger" id="timerStopBtn" type="button" title="Stop & log time">
              <i class="fas fa-stop"></i>
            </button>
            <span class="text-muted small fw-semibold" id="globalTimerDisplay">00:00:00</span>
          </div>
          <button class="btn btn-sm btn-outline-success" id="globalAddTimeBtn">
            <i class="fas fa-clock me-1"></i>Add Time
          </button>
          <div class="notif-toggle" id="notifToggle">
            <i class="fas fa-bell text-primary"></i>
          </div>
          <div class="notif-panel d-none" id="notifPanel">
            <div class="notif-panel-header">
              <div class="d-flex align-items-center gap-2">
                <i class="fas fa-bell text-primary"></i>
                <span class="fw-semibold">Notifications</span>
              </div>
              <span class="badge badge-soft-info" id="notifCount"><%= typeof notifCount !== 'undefined' ? notifCount : 0 %></span>
            </div>
            <div class="notif-list" id="notifList">
              <% const notifItems = typeof notifications !== 'undefined' ? notifications : []; %>
              <% if (notifItems.length > 0) { %>
                <% notifItems.forEach(n => { %>
                  <div class="notif-item">
                    <div class="notif-dot" style="background:<%= n.type === 'approval' ? '#4f46e5' : '#f59e0b' %>"></div>
                    <div class="flex-grow-1">
                      <div class="fw-semibold"><%= n.title %></div>
                      <div class="text-muted small"><%= n.subtitle %> · <%= n.meta %></div>
                    </div>
                    <% if (n.link) { %>
                      <a href="<%= n.link %>" class="btn btn-sm btn-outline-primary">Open</a>
                    <% } %>
                  </div>
                <% }) %>
              <% } else { %>
                <div class="p-3 text-muted text-center">No new notifications</div>
              <% } %>
            </div>
          </div>
          <script>
            window.__APP_NOTIFS__ = <%- JSON.stringify(typeof notifications !== 'undefined' ? notifications : []) %>;
          </script>
        </div>
      </nav>

      <!-- Global Add Time Modal -->
      <div class="modal fade" id="globalAddTimeModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title">Add Time Entry</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form method="POST" action="/cost-journal/time">
              <div class="modal-body">
                <div class="mb-3">
                  <label class="form-label">Date</label>
                  <input type="date" class="form-control" name="date" id="globalAddTimeDate" required>
                </div>
                <div class="mb-3">
                  <label class="form-label">Matter</label>
                  <select class="form-select" name="matter_id" id="globalAddTimeMatter">
                    <option value="">Loading...</option>
                  </select>
                </div>
                <div class="mb-3">
                  <label class="form-label">Description</label>
                  <textarea class="form-control" name="description" rows="2" required></textarea>
                </div>
                <div class="row g-2">
                  <div class="col-md-6 mb-3">
                    <label class="form-label">Hours</label>
                    <input type="number" class="form-control" name="hours" id="globalAddTimeHours" step="0.01" min="0" required>
                  </div>
                  <div class="col-md-6 mb-3">
                    <label class="form-label">Billable?</label>
                    <select class="form-select" name="is_billable">
                      <option value="true">Yes</option>
                      <option value="false">No</option>
                    </select>
                  </div>
                </div>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-primary">Add Time</button>
              </div>
            </form>
          </div>
        </div>
      </div>

      <!-- Flash Messages -->
      <% if (typeof error !== 'undefined' && error && error.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show m-4 shadow-sm" role="alert">
          <%= error %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% } %>

      <% if (typeof success !== 'undefined' && success && success.length > 0) { %>
        <div class="alert alert-success alert-dismissible fade show m-4 shadow-sm" role="alert">
          <%= success %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% } %>

      <!-- Page Content -->
